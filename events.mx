# If tmux is running, we split the window several times with useful information
# on the active command in the interactive terminal. This is triggered via
# pressing F1.

event onKeyPress cls={^L} {
    out: <stdin> test
}
